<link href='https://fonts.googleapis.com/css?family=Permanent+Marker' rel='stylesheet' type='text/css'>
<link href='https://fonts.googleapis.com/css?family=Dosis:400,800' rel='stylesheet' type='text/css'>

<div class="category-container">
<div class="body">

<h1> <%= @category.topic %> </h1>

    <div class="post-scroll">
        <% @category.posts.each do |post| %>
        <div><%= post.description %></div>

        <div class='post-user'>
        <%= post.user.user_name %>
    </div>

    <div class='edit-tab' >
        <%= link_to 'Edit Post', edit_post_path([post.id]), method: :get, class: "btn btn-default btn-xs" %>
    </div>
    <%end%>
    </div>
</select>

    <div class="description">
        <%= form_for @post do |f| %>
        <% flash.each do |key, msg| %>
    <div><%= msg %></div>
    <% end %>

        <%= f.hidden_field :category_id, :value => params[:id] %>

        <%= f.hidden_field :user_id, :value => session[:user_id] %>

        <%= f.text_area :description %>
    <h3>Share Something GOOD!</h3>
    <div class='post-button'>
        <%= f.submit "Post", class: "btn btn-default btn-sm" %>
    </div>
    </div>
    <% end %>
</div>
</div>
